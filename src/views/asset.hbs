<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title></title>
</head>
<body style="width: {{ params.width }}; height: {{ params.height }}">
  <img src="{{ imageSource }}" width="{{ params.width }}" height="{{ params.height }}" />
</body>
</html>